# disease-prediction-LLM
A fine-tuned LLM-based disease prediction model trained on symptom datasets. Includes JSONL data preparation, partial model training, test predictions, confusion matrix, and demo inference as required by the assignment.
